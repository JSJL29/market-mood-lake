FROM python:3.11-slim

WORKDIR /app

COPY build/requirements-api.txt /app/build/requirements-api.txt
RUN pip install --no-cache-dir -r /app/build/requirements-api.txt

COPY src/ /app/src/
COPY scripts/ /app/scripts/

CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
